import React,{useEffect,useMemo,useState} from 'react';
import {createRoot} from 'react-dom/client';import {Search,Plus,Mountain,Sparkles,Clock3,Flame,ChevronRight,X,Star,MessageCircle} from 'lucide-react';import {supabase} from './lib/supabase';import './style.css';
const types=['all','Breakfast','Lunch','Dinner','Snack'];
function Stars({value=0,onPick,small=false}){return <span className="stars">{[1,2,3,4,5].map(n=><button type="button" key={n} onClick={()=>onPick?.(n)} className={n<=Math.round(value)?'on':''}><Star size={small?13:17} fill="currentColor"/></button>)}</span>}
function App(){const [meals,setMeals]=useState([]),[ratings,setRatings]=useState([]),[comments,setComments]=useState([]),[query,setQuery]=useState(''),[filter,setFilter]=useState('all'),[selected,setSelected]=useState(null),[adding,setAdding]=useState(false),[author,setAuthor]=useState(()=>localStorage.getItem('disco-author')||''),[raterId]=useState(()=>{let id=localStorage.getItem('disco-rater-id');if(!id){id=crypto.randomUUID();localStorage.setItem('disco-rater-id',id)}return id}),[comment,setComment]=useState(''),[error,setError]=useState(''),[live,setLive]=useState(false);
async function load(){const [m,r,c]=await Promise.all([supabase.from('meals').select('*').order('created_at',{ascending:false}),supabase.from('meal_ratings').select('*'),supabase.from('meal_comments').select('*').order('created_at',{ascending:true})]);if(m.error)setError(m.error.message);else setMeals(m.data||[]);if(!r.error)setRatings(r.data||[]);if(!c.error)setComments(c.data||[])}
useEffect(()=>{load();const ch=supabase.channel('meal-live').on('postgres_changes',{event:'*',schema:'public',table:'meals'},load).on('postgres_changes',{event:'*',schema:'public',table:'meal_ratings'},()=>{load();setLive(true);setTimeout(()=>setLive(false),1500)}).on('postgres_changes',{event:'*',schema:'public',table:'meal_comments'},load).subscribe();return()=>supabase.removeChannel(ch)},[]);
const stats=id=>{const a=ratings.filter(x=>x.meal_id===id);return {count:a.length,avg:a.length?a.reduce((s,x)=>s+x.rating,0)/a.length:0,mine:a.find(x=>x.rater_id===raterId)?.rating||0}};
async function rate(id,n){const {error}=await supabase.from('meal_ratings').upsert({meal_id:id,rater_id:raterId,rating:n,rater_name:author||null},{onConflict:'meal_id,rater_id'});if(error)setError(error.message);else load()}
async function postComment(e){e.preventDefault();if(!author.trim()||!comment.trim())return;const {error}=await supabase.from('meal_comments').insert({meal_id:selected.id,author_name:author.trim(),comment:comment.trim()});if(error)setError(error.message);else{localStorage.setItem('disco-author',author.trim());setComment('');load()}}
async function add(e){e.preventDefault();const f=new FormData(e.currentTarget),name=String(f.get('author')).trim();localStorage.setItem('disco-author',name);setAuthor(name);const row={title:f.get('title'),chef_name:name,user_id:null,rating:Number(f.get('rating')||5),prep_time_minutes:Number(f.get('prep')||0),cook_time_minutes:Number(f.get('cook')||0),cook_method:f.get('method'),trip_type:f.get('trip'),meal_type:f.get('meal_type'),dietary_tags:[],calorie_density:'med',pack_weight_rating:3,ingredients:(f.get('ingredients')||'').split('\n').filter(Boolean),steps:(f.get('steps')||'').split('\n').filter(Boolean),tips:f.get('tips')||''};const {data,error}=await supabase.from('meals').insert(row).select().single();if(error)setError(error.message);else{if(row.rating)await supabase.from('meal_ratings').insert({meal_id:data.id,rater_id:raterId,rating:row.rating,rater_name:name});setAdding(false);load()}}
const visible=useMemo(()=>meals.filter(m=>(m.title+m.chef_name).toLowerCase().includes(query.toLowerCase())&&(filter==='all'||m.meal_type===filter)),[meals,query,filter]);const close=()=>{setSelected(null);setAdding(false);setError('')};
return <div className="app"><header><div className="brand"><div className="mark"><Mountain size={22}/></div><div><b>disco<span>-food</span></b><small>DISCOVERY 10 FOOD HANDBOOK</small></div></div><button className="post" onClick={()=>setAdding(true)}><Plus size={18}/> Post meal</button></header><main><section className="hero"><div><p className="eyebrow"><Sparkles size={15}/> FIELD NOTES FOR GOOD EATING</p><h1>Good food.<br/><i>Wild places.</i></h1><p className="intro">A shared recipe book for the Discovery 10 crew — built around campfires, coastlines, and the meals that get us there.</p><div className="stats"><span><b>{meals.length}</b> field recipes</span><span><b>4</b> meal types</span><span><b>∞</b> good stories</span></div></div><div className="hero-art"><div className="sun"/><Mountain size={170} strokeWidth={1}/><div className="ridge"/></div></section><section className="toolbar"><div className="search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search meals, ingredients, cooks..."/></div><div className="filters">{types.map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div></section>{live&&<div className="live">Ratings updated live</div>}{error&&<div className="live">{error}</div>}{visible.length?<section className="grid">{visible.map(m=>{const s=stats(m.id),cm=comments.filter(x=>x.meal_id===m.id).length;return <article className="card" key={m.id} onClick={()=>setSelected(m)}><div className={'card-top '+m.trip_type}><span>{m.meal_type}</span><span>{m.trip_type}</span></div><div className="card-body"><div className="tagline"><Stars value={s.avg} small/> {s.count?`${s.avg.toFixed(1)} (${s.count})`:'No ratings yet'}</div><h2>{m.title}</h2><p>Shared by <b>{m.chef_name}</b></p><p><MessageCircle size={14}/> {cm} comment{cm===1?'':'s'}</p><div className="meta"><span><Clock3 size={15}/>{m.prep_time_minutes+m.cook_time_minutes} min</span><span><Flame size={15}/>{m.cook_method}</span><ChevronRight size={18}/></div></div></article>})}</section>:<div className="empty"><h2>No meals posted yet.</h2><p>Be the first from Discovery 10 to drop a trail recipe!</p><button className="post" onClick={()=>setAdding(true)}><Plus size={17}/> Post the first meal</button></div>}</main>{(selected||adding)&&<div className="overlay" onClick={e=>e.target===e.currentTarget&&close()}>{selected?<div className="modal detail"><button className="close" onClick={close}><X/></button><div className="detail-kicker">{selected.meal_type} · {selected.trip_type}</div><h2>{selected.title}</h2><p className="byline">Shared by {selected.chef_name}</p><div className="rating-box"><Stars value={stats(selected.id).mine||stats(selected.id).avg} onPick={n=>rate(selected.id,n)}/> {stats(selected.id).count?`${stats(selected.id).avg.toFixed(1)} ★ (${stats(selected.id).count} ratings)${stats(selected.id).mine?` · Your rating: ${stats(selected.id).mine}`:''}`:'Be the first to rate this meal'}</div><div className="detail-cols"><div><h3>Pack list</h3>{(selected.ingredients||[]).map((x,i)=><label className="check" key={i}><input type="checkbox"/>{typeof x==='string'?x:x.name}</label>)}<h3>Trail tip</h3><p className="tip">{selected.tips}</p></div><div><h3>Make it happen</h3><ol>{(selected.steps||[]).map((x,i)=><li key={i}>{x}</li>)}</ol></div></div><h3>Comments</h3><div className="comments">{comments.filter(x=>x.meal_id===selected.id).map(x=><p key={x.id}><b>{x.author_name}</b> {x.comment}</p>)}</div><form className="comment-form" onSubmit={postComment}><input required value={author} onChange={e=>setAuthor(e.target.value)} placeholder="Your name"/><input required value={comment} onChange={e=>setComment(e.target.value)} placeholder="Add a comment..."/><button className="submit">Comment</button></form></div>:<form className="modal form" onSubmit={add}><button type="button" className="close" onClick={close}><X/></button><div className="detail-kicker">ADD TO THE HANDBOOK</div><h2>Post a meal</h2><input name="author" required value={author} onChange={e=>setAuthor(e.target.value)} placeholder="Your name"/><input name="title" required placeholder="Meal name"/><div className="two"><select name="meal_type"><option>Breakfast</option><option>Lunch</option><option>Dinner</option><option>Snack</option></select><input name="rating" type="number" min="1" max="5" defaultValue="5" placeholder="Initial stars (1-5)"/></div><div className="two"><input name="prep" type="number" placeholder="Prep min"/><input name="cook" type="number" placeholder="Cook min"/></div><div className="two"><select name="method"><option>one-pot</option><option>camp stove</option><option>fire</option><option>no-cook</option></select><select name="trip"><option>backpacking</option><option>kayaking</option><option>basecamp</option><option>snowshoeing</option></select></div><textarea name="ingredients" required placeholder="Ingredients, one per line"/><textarea name="steps" required placeholder="Steps, one per line"/><textarea name="tips" placeholder="Trail tips"/><button className="submit">Add to the field guide <Plus size={17}/></button></form>}</div>}</div>}
createRoot(document.getElementById('root')).render(<App/>);